{
  "version": 1,
  "description": "Tâches en retard ou à échéance atteinte",
  "_note_scan": "Enumeration explicite des sous-dossiers de pages/ = repli TRANSITOIRE (dette) : le moteur de requetes du serveur MCP ne scanne qu'un seul niveau, donc tout sous-dossier absent de cette liste reste invisible. La cle \"recursive\" est inerte sur le serveur actuel et prend le relais des le deploiement du support de scan.recursive (PR christian/obsidian-mcp #5) ; l'enumeration pourra alors etre retiree.",
  "parameters": {
    "types": {
      "type": "array",
      "items": { "enum": ["due", "scheduled", "start"] },
      "default": ["due", "scheduled", "start"],
      "description": "Types de dates à vérifier"
    }
  },
  "scan": {
    "folders": [
      "journals",
      "pages",
      "pages/1. Projets",
      "pages/1. Projets/Travaux",
      "pages/2. Domaines",
      "pages/3. Ressources"
    ],
    "recursive": true,
    "file_pattern": "*.md"
  },
  "filter": {
    "task_open": true,
    "date_conditions": {
      "due":       { "emoji": "📅", "operator": "<=", "value": "today" },
      "scheduled": { "emoji": "⏳", "operator": "<=", "value": "today" },
      "start":     { "emoji": "🛫", "operator": "<=", "value": "today" }
    }
  },
  "group_by": "date_type",
  "sort": { "field": "date", "order": "asc" }
}
